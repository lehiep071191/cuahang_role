<% @users.each do |user|%>
    <ul>    
        <% if user.role.role_name != "super_admin"%>
            <li>
                <%= user.name%>
                <%= link_to 'edit', edit_admin_user_path(user) %>
            </li>
        <%end%>    
    </ul>
<% end %>